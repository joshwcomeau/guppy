// @flow
import { defineMessages } from 'react-intl';

const messages = {
  main: {
    title: {
      id: 'dependencyManagementPane.addDependencyModal.main.title',
      defaultMessage: 'Add New Dependency',
      description: 'Text for the title "Add New Dependency"',
    },
  },
};

const definedMessages = defineMessages(messages);

export default definedMessages;
